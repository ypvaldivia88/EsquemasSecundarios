@model IEnumerable<EsquemasSecundarios.Models.Plantilla>

@{
    ViewBag.Title = "Plantillas de relevadores";
    ViewBag.PTitle = "Listado";
}

<div class="row">
    <div class="col-md-12">

        <table id="datatable-buttons" class="table table-striped table-bordered table-hover">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.Modelo)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Fabricante)
                    </th>
                    <th></th>
                </tr>
            </thead>            
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Modelo)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Fabricante.Nombre)
                        </td>
                        <td style="width: 40px;">
                            <a href="@Url.Action("Details","Plantillas", new { id = item.id_Plantilla } )" class="btn btn-info btn-xs" title="Detalles"><i class="fa fa-eye"></i>  </a>
                        </td>
                    </tr>
                }
            </tbody>
            

        </table>
    </div>
</div>

@section styles {
    @Styles.Render("~/Style/DataTables")
}

@section script {
    @Scripts.Render("~/Script/DataTables")
}